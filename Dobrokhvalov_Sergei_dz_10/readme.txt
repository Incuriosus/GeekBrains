№1: логика в инициализаторе проверяет соответствие приехавшего объекта матрице
(список равных списков, в которых объекты типа int). Не придумал как вывести матрицы супер ровно.
№2: в задании необходимо посчитать расход ткани, поэтому метод возвращает число (суммирует атрибуты всех объектов)
Не стал кешировать необходимое количество ткани для каждого объекта, т.к. вдруг размер для него поменяется.
